---
filebeat_version: "7.14.1"
filebeat_install_type: remote
elastic_instance: 0.0.0.0
kibana_instance: 0.0.0.0
# ansible 2.8 не понимает такую конструкцию elastic_instance: "{{ hostvars['el-instance']['ansible_facts']['default_ipv4']['address'] | default('0.0.0.0') }}"
# ansible 2.8 не понимает такую конструкцию kibana_instance: "{{ hostvars['kbn-instance']['ansible_facts']['default_ipv4']['address'] | default('0.0.0.0') }}"
